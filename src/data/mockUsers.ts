/**
 * DEPRECATED — this file is no longer used.
 *
 * Accounts are now real Supabase Auth users (see src/context/AuthContext.tsx
 * and supabase/schema.sql) instead of an in-memory/localStorage mock
 * database. This file is kept as an empty stub only because this sandbox's
 * filesystem doesn't allow deleting files; nothing in the app imports it.
 */
export {};
