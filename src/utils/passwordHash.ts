/**
 * DEPRECATED — this file is no longer used.
 *
 * Authentication is now handled by Supabase Auth (see
 * src/context/AuthContext.tsx), which does real server-side password
 * hashing. Client-side hashing is no longer needed. Kept as an empty stub
 * because this sandbox's filesystem doesn't allow deleting files.
 */
export {};
